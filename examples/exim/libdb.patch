diff --git a/src/os/os_stack.c b/src/os/os_stack.c
index 037080f..226666d 100644
--- a/src/os/os_stack.c
+++ b/src/os/os_stack.c
@@ -24,7 +24,7 @@ void
 __os_stack(env)
 	ENV *env;
 {
-#if defined(HAVE_BACKTRACE) && defined(HAVE_BACKTRACE_SYMBOLS)
+#if 0 && defined(HAVE_BACKTRACE) && defined(HAVE_BACKTRACE_SYMBOLS)
 	void *array[200];
 	size_t i, size;
 	char **strings;
